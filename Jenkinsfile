pipeline {
    agent any
    stages{
        stage('git clone') {
            steps{
                // cloning git repo 
                sh 'git clone https://github.com/naveen-e-devops/VProfile.git'
            }

        }
    }
}
